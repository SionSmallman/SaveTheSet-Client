import LoginButton from "../AuthComponents/LoginButton";

function SetlistPageLogin() {
  return (
    <div id="form-login" className="my-5 text-center">
      <p>Log in using Spotify to customise playlist!</p>
      <LoginButton />
    </div>
  );
}

export default SetlistPageLogin;
